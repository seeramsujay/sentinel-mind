Got it. Since the project repository will be evaluated by an AI, technical consistency, explicit architectural boundaries, and clear evidence of real-world constraints (like your proxy user feedback and backtesting) are paramount. AI evaluators look heavily for logical flow between problem, architecture, and code execution. 

Here are the completely updated technical stack markdown files, integrating the exact React and architecture changes.

### File 1: System Designer & Cloud Manager (You)

```markdown
# Role: System Designer, Cloud Manager & Meta-Orchestrator
**Assignee:** Google Services Specialist (Lead)
**System Domain:** "The Brain" & Bias Shield

## 1. Technical Stack
* **Cloud Infrastructure:** Google Cloud Platform (GCP).
* **Orchestration Engine:** Vertex AI Agent Builder.
* **Monitoring & Governance:** Vertex AI Model Monitoring.
* **Core APIs (Admin):** Gemini APIs, Google Maps Routes API, Firestore API.
* **Language:** Python.

## 2. Technical Documentation & Responsibilities

### Infrastructure Provisioning
* Provision the foundational GCP Project.
* Enable all necessary APIs (Vertex AI, Gemini, Maps, Firestore).
* Generate, secure, and distribute IAM Service Accounts and API keys to Krishna, Lochan, and Aditya.

### The Meta-Orchestrator Logic & Conflict Resolution
* Build the "Watcher" Python daemon to continuously listen to the Firestore `/emergencies` collection.
* [cite_start]**Proxy User Logic (Crucial):** Based on official NDMA Post-Mortem After-Action Reports, duplicate panic calls inherently misroute ambulances[cite: 235, 236]. [cite_start]You must architect the Meta-Orchestrator to autonomously merge duplicate coordinates before dispatch[cite: 237].
* Implement asynchronous intervention: The orchestrator must only activate when the database hits a `status == "conflict"` state.
* Use Vertex AI Agent Builder to autonomously evaluate and resolve resource overlaps (e.g., re-routing ambulances for high-priority SOS alerts). Update the database state back to `dispatched` post-resolution.

### The Fairness & Governance Layer
* Integrate Vertex AI Model Monitoring to run a "fairness check" on all AI dispatch decisions.
* Guarantee decisions avoid bias based on demographic or socio-economic proxies.
* Log the exact reasoning for dispatch decisions into the `fairness_audit` database key for Krishna's React UI.
```

---

### File 2: Frontend Developer (Krishna)

```markdown
# Role: UI/UX & Communication Lead
**Assignee:** Krishna
**System Domain:** "The Glass", Voice & Explainability

## 1. Technical Stack
* [cite_start]**Framework:** React Web[cite: 227].
* **UI Library:** MUI or Tailwind (Landscape-only Command Center layout).
* [cite_start]**Mapping Library:** `@react-google-maps/api`[cite: 228].
* [cite_start]**State Management:** Native Firebase `onSnapshot()` listeners inside `useEffect` hooks[cite: 228].
* [cite_start]**Deployment:** Firebase Hosting[cite: 229].

## 2. Technical Documentation & Responsibilities

### UI Architecture & Constraints
* [cite_start]Build a fast, production-grade React Web dashboard designed strictly as a real-time Enterprise Command Center[cite: 227, 240].
* **CRITICAL CONSTRAINT:** Do not use polling loops. [cite_start]You must use native Firebase `onSnapshot()` listeners inside `useEffect` hooks to listen to Firestore state changes[cite: 228]. [cite_start]This ensures live asynchronous updates instantly without page reloads[cite: 241].
* [cite_start]Deploy the application globally via Firebase Hosting[cite: 229, 241].

### Visual Mapping & Dashboards
* [cite_start]Connect the `@react-google-maps/api` library to dynamically render live disaster coordinates and polyline emergency routes mapped by Role 3[cite: 228].
* Ensure the UI looks like a professional B2B dispatch tool, not a consumer mobile app.

### Transparency-as-a-Service Panel
* Build the Explainability Feed UI panel.
* [cite_start]Listen specifically to the `fairness_audit` key generated by Role 1 and display the Orchestrator's reasoning live to prove to judges why a routing choice was made, avoiding "black box" penalties[cite: 230].

### AI Interactive Elements
* [cite_start]**RAG SitReps:** Set up the UI container to display auto-generated Situation Reports drafted by Gemini 1.5 Pro[cite: 231].
* [cite_start]**Vision Flex:** Build a manual "Upload Field Image" React component[cite: 232]. [cite_start]Wire it to instantly feed mock drone or satellite imagery to Gemini 1.5 Pro Vision to classify damage severity[cite: 232].
```

---

### File 3: Backend / ML Developer (Lochan)

```markdown
# Role: Triage Ingestion, Logistics & Prediction Lead
**Assignee:** Lochan
**System Domain:** "The Filter", "The Muscle" & Forecaster

## 1. Technical Stack
* **Languages & Libraries:** Python, `feedparser`.
* **LLMs:** Gemini 1.5 Flash (Ingestion), Gemini 1.5 Pro (RAG/Heuristics), Gemini 1.5 Pro Vision (Image Analysis).
* **ML & Vector Search:** Vertex AI AutoML Tabular, Vertex AI Vector Search.
* **External APIs:** Google Maps Routes API, OpenWeatherMap API.

## 2. Technical Documentation & Responsibilities

### Performance & Backtesting Target
* [cite_start]**Simulation Constraint:** We are hardcoding historical NDMA flood data from the Andhra Pradesh region into the pipeline to backtest SentinelMind[cite: 222].
* [cite_start]**Speed Metric:** Your scripts must ensure the swarm can process and route 100 simultaneous distress signals in under 5 seconds (compared to the standard 4-6 hour manual window)[cite: 223].

### Triage & Data Ingestion
* Write a lightweight Python script to pull live unstructured RSS alerts from disaster monitors.
* Pass unstructured text to Gemini 1.5 Flash using the `response_schema` parameter to force the LLM to output strict JSON (location, hazard type, urgency P1/P2/P3, and `status: "triaged"`).

### Logistics & Routing
* Create a loop that triggers only when an emergency document is `status == "triaged"`.
* Query the Google Maps Routes API to locate the closest available rescue vehicle from Aditya's `/resources` collection.
* Generate the polyline route, calculate ETA, and command the database to update the document to `dispatched`.

### Machine Learning & Risk Forecasting
* Call a Vertex AI AutoML Tabular endpoint to calculate mathematical crisis spread probability.
* Pull live weather data via OpenWeatherMap API, feeding it to Gemini 1.5 Pro to generate a text-based "6-Hour Risk Assessment".

### RAG & Vision Analysis
* [cite_start]Load historical NDMA disaster PDFs into Vertex AI Vector Search[cite: 231].
* [cite_start]Listen for `dispatched` events and use Gemini 1.5 Pro alongside the Vector Search to autonomously draft professional Situation Reports (SitReps)[cite: 231].
```

---

### File 4: Database & API Connection (Aditya)

```markdown
# Role: Database Admin & Data Contract Manager
**Assignee:** Aditya
**System Domain:** The Universal Data Contract & State Management

## 1. Technical Stack
* **Database Engine:** Firebase Firestore (Cloud-native state layer).
* **Data Serialization:** Strict JSON Formats.
* **Frontend Connection:** WebSockets/`onSnapshot()` integration for React.

## 2. Technical Documentation & Responsibilities

### The Universal Data Contract
* Establish and enforce the exact JSON schema that all agents will use.
* Ensure agents never communicate directly; all communication occurs asynchronously by reading/writing to Firestore.

### Database Schema Design
* Design and maintain the `/emergencies` collection (populated by Lochan's ingestion pipeline).
* Design and maintain the `/resources` collection, populating it with mock emergency vehicles, unit IDs, and starting coordinates.
* [cite_start]Implement the `fairness_audit` key structure mandated for the Meta-Orchestrator to write to, and for Krishna's React app to read from[cite: 230].

### Front-End Integration Architecture
* Ensure database security rules and index optimizations support Krishna's React Web dashboard.
* [cite_start]The frontend will utilize native Firebase `onSnapshot()` listeners tied to the `/emergencies` and `/resources` collections to render live asynchronous updates instantly without page reloads[cite: 228, 241]. [cite_start]Ensure write speeds from the backend do not breach Firestore rate limits during the 100-signal/5-second stress test[cite: 223].

### The State-Machine Pipeline
* Define and enforce the exact status flow to prevent infinite loop race conditions.
* **Pipeline Status Flags:** `received` -> `triaged` -> `awaiting_dispatch` -> `dispatched` -> `conflict` -> `recovery`.
```